<% if user_signed_in? %>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top border-bottom border-primary">
    <div class="container">
      <%= link_to "NationalCakeGame", root_path, class: "navbar-brand fw-bold text-white m-2" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarToggler">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if current_user.admin? %>
            <li class="nav-item m-6"><%= link_to "Dashboard", admin_root_path, class: "nav-link text-white" %></li>
            <li class="nav-item m-6"><%= link_to "Events", admin_events_path, class: "nav-link text-white" %></li>
            <li class="nav-item m-6"><%= link_to "Feedbacks", admin_event_feedbacks_path(Event.first), class: "nav-link text-white" %></li>
            <li class="nav-item m-6"><%= link_to "Business Profiles", admin_business_profiles_path, class: "nav-link text-white" %></li>
            <li class="nav-item m-6"><%= link_to "Registrations", admin_event_event_registrations_path(Event.first), class: "nav-link text-white" %></li>
          <% else %>
            <li class="nav-item"><%= link_to "Dashboard", dashboard_path, class: "nav-link text-white" %></li>
          <% end %>
        </ul>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
              <%= image_tag(current_user.avatar.attached? ? url_for(current_user.avatar) : "default-avatar.png", class: "rounded-circle me-2", width: 32, height: 32, alt: "User Avatar") %>
              <%= current_user.name.presence || "My Profile" %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li><%= link_to "Profile", profile_path(current_user.id), class: "dropdown-item" %></li>
              <li><%= link_to "Settings", edit_user_registration_path, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "dropdown-item", data: { turbo: false } %></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>
<% end %>
